{% extends 'admin/base.html.twig' %}
{% import "admin/macros/sort-link.html.twig" as sortLink %}
{% block title %}Reservation details
{% endblock %}

{% block body %}
	<div class="flex flex-row justify-between">
		<h1 class="page-title font-bold">Reservation details</h1>
		<a href="{{ path('reservation_edit', { id: details.id, currentPage: currentPage }) }}" class="button button-primary">Edit</a>
	</div>
	{% for message in app.flashes('notice') %}
		<div class="flash-notice">
			{{ message }}
		</div>
	{% endfor %}
	<div class="rounded-lg border border-gray-200 mt-4">
		<div class="overflow-x-auto rounded-t-lg">
			<table class="table-details min-w-full lg:min-width-80 divide-y-2 divide-gray-200 bg-white text-sm overflow-x-auto">
				<thead class="text-left rtl:text-right">
					<tr class="h-8">
						<th class="ps-2">
							Key
						</th>
						<th>
							Value
						</th>
					</tr>
				</thead>
				<tbody class="divide-y leading-8 divide-gray-200">
					<tr>
						<td class="font-normal">
							Date from
						</td>
						<td class="font-normal">
							{{ details.dateFrom|date('d/m/Y') }}
						</td>
					</tr>
					<tr>
						<td class="font-normal">
							Date to
						</td>
						<td class="font-normal">
							{{ details.dateTo|date('d/m/Y') }}
						</td>
					</tr>
					<tr>
						<td class="font-normal">
							Adults
						</td>
						<td class="font-normal">
							{{ details.adults }}
						</td>
					</tr>
					<tr>
						<td class="font-normal">
							Children
						</td>
						<td class="font-normal">
							{{ details.children }}
						</td>
					</tr>
					<tr>
						<td class="font-normal">
							Customer
						</td>
						<td class="font-normal">{{ details.customer.firstName }}
							{{ details.customer.lastName }}</td>
					</tr>
					<tr>
						<td class="font-normal">
							Date added
						</td>
						<td class="font-normal">
							{{ details.dateCreated|date("d/m/Y") }}
						</td>
					</tr>
					<tr>
						<td class="font-normal">
							Room number
						</td>
						<td class="font-normal">
						{{ details.roomEntity.id }}
						</td>
					</tr>
				</tbody>
			</table>
		</div>
	</div>
	{{ include('admin/partials/modal-confirm.html.twig', { message: 'Are you sure you want to delete this room?' }) }}
{% endblock %}
